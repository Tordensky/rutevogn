<div class="page-title">Bussen min går om</div>
<div id="departure-in" class="travel-info-counter">{{nextDepartureTime}}</div>
<div class="page-title">sentrum - universitetet</div>

<div class="travel-info-container">
    <table class="travel-info-table">
    {{#routes}}
        <tr class="travel-info-entry">
            <td>{{route}}</td>
            <td>{{departureTime}}</td>
            <!-- IF YOU WANT TO SHOW DEPARTURE TIME <td>{{arrivalTime}}</td>-->
            <td>{{travelTime}}</td>
        </tr>
    {{/routes}}
    </table>
</div>

<div class="page-button-container">
    <button id="new-search-button" class="page-button">Nytt søk</button>
</div>