<div class="page-title">{{title}}</div>

<div class="buss-stops-container">
    {{#stops}}
        <button data-id="{{_id}}" data-name="{{name}}" class="buss-stop-button departure">{{name}}</button>
    {{/stops}}
    <br class="clear" />
</div>
