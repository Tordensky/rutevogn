var Basic = Basic || {};

Basic.DepartureView = Backbone.View.extend({
    initalize: function() {
        console.log("init main view");
    },

    render: function() {
        console.log("render main view")
    }
});
